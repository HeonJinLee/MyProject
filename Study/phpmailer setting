1. phpmailer 설치!
phpmailer니까 당연히 PHP가 설치되어 있어야겠죠??
  dnf install php
  dnf install git
  git clone https://github.com/PHPMailer/PHPMailer


2. test.php (phpmailer 동작시킬 코드 작성)

<?php
use PHPMailer\PHPMailer\PHPMailer;
use PHPMailer\PHPMailer\Exception;
use PHPMailer\PHPMailer\SMTP;

require './PHPMailer/src/Exception.php';
require './PHPMailer/src/PHPMailer.php';
require './PHPMailer/src/SMTP.php';

$mail = new PHPMailer(true);
$mail->IsSMTP(); 

try {
    $mail->CharSet = "utf-8";
    $mail->Encoding = "base64";

    $mail->Host = "smtp.gmail.com";
    $mail->SMTPAuth = true;
    $mail->Port = 465;
    $mail->SMTPSecure = "ssl";
    $mail->Username   = "test@test.com";    //gmail 계정
    $mail->Password   = "testtest!";        //gmail 패스워드

    $mail->SetFrom('test@gmail.com', 'Hacker'); //발신자 정보 (보내는사람 주소는 추가 셋팅을 하지 않으면 Username으로 발송)
    $mail->AddAddress('testReceived@domain.com', 'Alice');  //수신자 정보
    $mail->Subject = 'Email test!';             //메일제목
    $mail->MsgHTML(file_get_contents('contents.html'));     //메일 본문

//    $mail->Body = "
//    <b>메일 내용</b>
//     <br/>
//     <br/>
//    <a target='_self' href='' >링크</a>
//    ";

    $mail->Send();

    echo "Message Sent OK \n";
}

catch (phpmailerException $e) {
    echo $e->errorMessage();
} catch (Exception $e) {
    echo $e->getMessage();
}
?>

3. contents.html 에 내가 메일본문에 넣을 내용을 작성!
